

int	ft_isalpha(int c)
{
    if ((c >= 'a' && c <= 'z')
    || (c >= 'A' && c <= 'Z'))
        return (1024);
    else
        return (0);
}
	// if (str[0] == 0)
	// {
	// 	return (1);
	// }	
	// return (1);
// }